$(function(){
     $('.random-text').okshadow({
       color: '#555',
       textShadow: true,
       xMax: 5,
       yMax: 0,
       yOffset: 3,
       fuzzMin: 1,
       fuzzMax: 3,
     });
   });
